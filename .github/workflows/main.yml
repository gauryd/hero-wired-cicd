name: Read find manually

on: 
   workflow_dispatch:

jobs: 
   read-file: 
     runs-on: ubuntu-latest

     steps:
        - name: Check Repository
          uses: actions/checkout@v4

        - name: Read app.txt file
          run: cat app.txt

